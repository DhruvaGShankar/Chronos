[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chronos-engine"
version = "2.0.0"
description = "Enterprise-Grade Schema-Driven Organizational Simulation Engine"
authors = [{ name = "Chronos Architecture Team" }]
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
chronos = "chronos.cli.main:cli_main"
